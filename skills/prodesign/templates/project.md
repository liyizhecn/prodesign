# {{PRODUCT}} · 产品上下文（唯一事实源）

> 本文件是产品级不变量的权威。每个版本工作区开工前必读；修改本文件需在 changelog 中说明。
> 创建于 {{DATE}}。

## 1. 产品定位

<一句话定位：目标客户 / 部署形态 / 核心价值。示例：企业 AI 流量统一控制平面（单企业私有部署，非 SaaS）。>

## 2. 版本史

| 版本 | 主题 | 模块 | 状态 |
|---|---|---|---|
| | | | |

## 3. 编号规范（本产品实例化）

- 模块号 M：下一个可用号见 registry/ids.md
- 功能点：`F<模块号>-<两位序号>`（如 F18-01）
- 用户故事：`US-<版本前缀>-<两位序号>`（如 US-V44-01）
- 架构决策：`A<版本号>-<序号>`（如 A4.4-1）
- 内容实体双命名空间（按需实例化）：内置 `SYS-xxxx` / 自定义 `USR-xxxx`
- <其他业务 ID 格式：如 事件 EVT-xxxxxxxx、密钥 vk-xxxx>

## 4. 角色与权限模型

<后台角色清单及边界。示例：超管 / 管理员 / 审计员（只读）。>

## 5. 原型全局约束

- 设计语言：<如 Antd 风格>；种子色（token seed 层）：<如 --seed-primary: #1677ff>
- 目标分辨率：<如 桌面端 1920×1080>
- 零外部依赖单文件 HTML（硬约束）；**页面唯一起点：deliverables/tools/_页面骨架.html**（token 三层 + 组件样式库，首版实例化）
- 菜单唯一事实源：deliverables/tools/_菜单模板.html；深链协议：`?focus=<实体ID>` 定位高亮
- token 纪律：色值只出现在骨架 :root；页面新增 token/组件样式先进骨架（validators 三查兜底）

## 6. 演示数据事实源清单

> 原型中出现的一切演示实体必须出自下表（或已交付原型），新实体先登记再使用。

| 实体类型 | 权威清单 | 备注 |
|---|---|---|
| 部门 | | |
| 人员 | | |
| 模型/提供商 | | |

## 7. 明确的产品级约束

<跨版本不变的硬约束。示例：私有化部署无外网依赖；审计数据不可删改。>

## 8. 术语表（一物一名权威）

> 同一概念全库只允许一个名字。更名必须在此登记（旧名进"曾用名"列），并全库替换后由评审抽查残留。

| 术语 | 定义 | 曾用名（废弃） |
|---|---|---|
| | | |
